---
phase: 01-funda-o-pipeline-de-dados
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - js/app.js
  - js/modules/format.js
  - js/modules/storage.js
  - js/modules/state.js
  - js/modules/data-service.js
  - js/views/dashboard.js
  - js/views/transaction.js
  - js/views/statement.js
  - js/views/receipt.js
  - js/views/settings.js
autonomous: true
requirements:
  - FUND-01
  - UX-02
  - UX-03

must_haves:
  truths:
    - "index.html carrega no browser sem erros de console"
    - "Sidebar desktop mostra 5 abas com ícones e labels: Dashboard, Transação, Extrato, Comprovante, Config"
    - "Clicar em cada aba mostra a seção correspondente e esconde as outras"
    - "Em viewport ≤768px, sidebar vira bottom bar com ícones sem labels"
    - "Formato brasileiro funciona: formatCurrency(1234.56) retorna 'R$ 1.234,56'"
    - "data-service.js lê JSON de data/ e retorna objetos parseados"
    - "Views placeholder renderizam card com ícone, heading e body text"
  artifacts:
    - path: "index.html"
      provides: "App shell com sidebar, 5 view sections, wizard overlay container"
      min_lines: 80
    - path: "js/app.js"
      provides: "Entry point — SPA navigation, view initialization"
      exports: ["navigate"]
    - path: "js/modules/format.js"
      provides: "Formatação brasileira (moeda, data, mês corrente)"
      exports: ["formatCurrency", "formatDate", "getCurrentYearMonth"]
    - path: "js/modules/storage.js"
      provides: "LocalStorage helpers para PAT, wizard state, pending writes"
      exports: ["getRepoConfig", "saveRepoConfig", "isWizardDone", "markWizardDone"]
    - path: "js/modules/state.js"
      provides: "Estado global in-memory da aplicação"
      exports: ["getState", "setState"]
    - path: "js/modules/data-service.js"
      provides: "Camada de acesso a dados via fetch + cache"
      exports: ["getConfig", "getCategories", "getPaymentMethods", "getTransactions", "invalidateCache"]
    - path: "js/views/dashboard.js"
      provides: "Placeholder da aba Dashboard"
      exports: ["initDashboard"]
    - path: "js/views/settings.js"
      provides: "Placeholder da aba Config"
      exports: ["initSettings"]
  key_links:
    - from: "index.html"
      to: "js/app.js"
      via: "script type=module src"
      pattern: "type=\"module\" src=\"js/app.js\""
    - from: "js/app.js"
      to: "js/views/*.js"
      via: "import { init* }"
      pattern: "import.*from.*views/"
    - from: "js/views/*.js"
      to: "js/modules/data-service.js"
      via: "import { get* }"
      pattern: "import.*from.*data-service"
---

<objective>
Criar o app shell completo: index.html com sidebar de navegação, módulos JavaScript core (navigation, formatação, localStorage, state, data service), e todas as views placeholder.

Purpose: Entregar uma aplicação funcional no browser — o usuário vê a sidebar, navega entre abas, e cada view mostra seu placeholder. Essa é a estrutura sobre a qual todas as features futuras (CRUD, wizard, write path) serão construídas.

Output: 11 arquivos — 1 HTML, 5 JS modules em js/modules/, 5 JS views em js/views/
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-funda-o-pipeline-de-dados/01-CONTEXT.md
@.planning/phases/01-funda-o-pipeline-de-dados/01-RESEARCH.md
@.planning/phases/01-funda-o-pipeline-de-dados/01-UI-SPEC.md

<interfaces>
<!-- CSS classes disponíveis do Plan 01 (pode rodar em paralelo — usar as mesmas classes) -->
Layout: .app-layout, .sidebar, .sidebar-header, .sidebar-nav, .nav-item, .nav-item.active, .nav-icon, .nav-label, .main-content
Components: .card, .btn, .btn-primary, .btn-ghost, .alert, .alert-success, .alert-error, .form-group, .form-label, .form-input, .spinner
Views: .view-section, .placeholder-view, .placeholder-icon, .wizard-overlay, .wizard-container
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar index.html — App Shell completo com sidebar e navegação</name>
  <files>index.html</files>

  <read_first>
    - .planning/phases/01-funda-o-pipeline-de-dados/01-UI-SPEC.md (Layout Contract — desktop e mobile, Component Inventory, Copywriting Contract)
    - .planning/phases/01-funda-o-pipeline-de-dados/01-RESEARCH.md (seção "Pattern 3: SPA Navigation com Sidebar" — VIEWS object, navigate function)
    - .planning/phases/01-funda-o-pipeline-de-dados/01-CONTEXT.md (decisões D-01, D-02, D-03, D-04, D-14, D-15, D-17)
  </read_first>

  <action>
Criar `index.html` na raiz do projeto com a seguinte estrutura:

**`<head>`:**
- `<meta charset="UTF-8">`
- `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
- `<title>FinanceiroVK</title>`
- Google Sans font via Google Fonts CDN: `<link href="https://fonts.googleapis.com/css2?family=Google+Sans:wght@400;700&display=swap" rel="stylesheet">` — se Google Sans não estiver disponível publicamente, usar `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">` como fallback e ajustar `--font-family` para `'Inter', Arial, sans-serif`
- CSS imports na ordem: `css/variables.css`, `css/base.css`, `css/components.css`, `css/views.css`
- `<script type="module" src="js/app.js"></script>` (per D-17)

**`<body>`:**
```html
<div class="app-layout">
  <!-- Sidebar (D-02: lateral desktop, D-03: bottom bar mobile) -->
  <aside class="sidebar">
    <div class="sidebar-header">💰 FinanceiroVK</div>
    <nav class="sidebar-nav">
      <button class="nav-item active" data-view="dashboard">
        <span class="nav-icon">📊</span>
        <span class="nav-label">Dashboard</span>
      </button>
      <button class="nav-item" data-view="transaction">
        <span class="nav-icon">💳</span>
        <span class="nav-label">Transação</span>
      </button>
      <button class="nav-item" data-view="statement">
        <span class="nav-icon">📋</span>
        <span class="nav-label">Extrato</span>
      </button>
      <button class="nav-item" data-view="receipt">
        <span class="nav-icon">📸</span>
        <span class="nav-label">Comprovante</span>
      </button>
      <button class="nav-item" data-view="settings">
        <span class="nav-icon">⚙️</span>
        <span class="nav-label">Config</span>
      </button>
    </nav>
  </aside>

  <!-- Main Content Area -->
  <main class="main-content">
    <section id="view-dashboard" class="view-section"></section>
    <section id="view-transaction" class="view-section" hidden></section>
    <section id="view-statement" class="view-section" hidden></section>
    <section id="view-receipt" class="view-section" hidden></section>
    <section id="view-settings" class="view-section" hidden></section>
  </main>
</div>

<!-- Wizard overlay (D-05 — populado por wizard.js no Plan 04) -->
<div id="wizard-overlay" class="wizard-overlay" hidden></div>

<!-- Alert container (para feedback global) -->
<div id="alert-container" style="position:fixed;top:16px;right:16px;z-index:300;display:flex;flex-direction:column;gap:8px;"></div>
```

Os 5 nav buttons devem usar `<button>` (não `<a>`) para acessibilidade — são ações de navegação SPA, não links. Cada button tem `data-view` correspondente ao id da section (sem o prefixo `view-`). As 5 abas são per D-01: Dashboard, Transação, Extrato, Comprovante, Config.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('index.html','utf8'); console.log('sidebar:', h.includes('class=\"sidebar\"')); console.log('5 views:', (h.match(/view-section/g)||[]).length); console.log('module:', h.includes('type=\"module\"')); console.log('wizard:', h.includes('wizard-overlay'));"</automated>
  </verify>

  <acceptance_criteria>
    - index.html existe na raiz com `<!DOCTYPE html>` e `<html lang="pt-BR">`
    - Contém `<link` para css/variables.css, css/base.css, css/components.css, css/views.css (4 links)
    - Contém `<script type="module" src="js/app.js">`
    - Contém `<aside class="sidebar">` com 5 `<button class="nav-item"` com data-view: dashboard, transaction, statement, receipt, settings
    - Contém 5 `<section id="view-*" class="view-section">` (dashboard, transaction, statement, receipt, settings)
    - Contém `<div id="wizard-overlay" class="wizard-overlay" hidden>`
    - Contém `<div id="alert-container"` para feedback global
    - Contém Google Sans font import (ou Inter como fallback)
    - Primeiro nav-item (dashboard) tem class "active", os outros não
    - Seção view-dashboard NÃO tem atributo `hidden`, as outras 4 TÊM
  </acceptance_criteria>

  <done>index.html existe com app shell completo, sidebar de 5 abas, 5 view sections, wizard overlay container, e imports de CSS/JS corretos</done>
</task>

<task type="auto">
  <name>Task 2: Criar módulos core JS — app.js, format.js, storage.js, state.js</name>
  <files>js/app.js, js/modules/format.js, js/modules/storage.js, js/modules/state.js</files>

  <read_first>
    - index.html (recém-criado — para ver IDs dos elementos, data-view attributes, estrutura DOM)
    - .planning/phases/01-funda-o-pipeline-de-dados/01-RESEARCH.md (seções: "Pattern 3: SPA Navigation", "Code Examples: Formatação Brasileira", "LocalStorage para PAT e Config do Repo")
    - .planning/phases/01-funda-o-pipeline-de-dados/01-CONTEXT.md (decisões D-16, D-17, D-18)
  </read_first>

  <action>
Criar 4 módulos ES Module (per D-17):

**1. `js/modules/format.js`** — Formatação brasileira (per D-16, UX-03). Copiar o padrão EXATO da seção "Code Examples: Formatação Brasileira" do RESEARCH.md:

```javascript
const currencyFormatter = new Intl.NumberFormat('pt-BR', {
  style: 'currency',
  currency: 'BRL'
});

const dateFormatter = new Intl.DateTimeFormat('pt-BR', {
  timeZone: 'America/Sao_Paulo'
});

const dateTimeFormatter = new Intl.DateTimeFormat('pt-BR', {
  timeZone: 'America/Sao_Paulo',
  year: 'numeric',
  month: '2-digit',
  day: '2-digit',
  hour: '2-digit',
  minute: '2-digit'
});

export function formatCurrency(value) {
  return currencyFormatter.format(value);
}

export function formatDate(dateStr) {
  return dateFormatter.format(new Date(dateStr + 'T12:00:00'));
}

export function formatDateTime(isoStr) {
  return dateTimeFormatter.format(new Date(isoStr));
}

export function getCurrentYearMonth() {
  const now = new Date();
  const y = now.getFullYear();
  const m = String(now.getMonth() + 1).padStart(2, '0');
  return `${y}-${m}`;
}
```

**2. `js/modules/storage.js`** — localStorage helpers. Copiar padrão da seção "LocalStorage para PAT e Config do Repo" do RESEARCH.md:

```javascript
const KEYS = {
  REPO_CONFIG: 'financeirovk_repo',
  WIZARD_DONE: 'financeirovk_wizard_done',
  PENDING_WRITES: 'financeirovk_pending'
};

export function getRepoConfig() {
  try {
    return JSON.parse(localStorage.getItem(KEYS.REPO_CONFIG)) || {};
  } catch {
    return {};
  }
}

export function saveRepoConfig(config) {
  localStorage.setItem(KEYS.REPO_CONFIG, JSON.stringify(config));
}

export function isWizardDone() {
  return localStorage.getItem(KEYS.WIZARD_DONE) === 'true';
}

export function markWizardDone() {
  localStorage.setItem(KEYS.WIZARD_DONE, 'true');
}

export function getPendingWrites() {
  try {
    return JSON.parse(localStorage.getItem(KEYS.PENDING_WRITES)) || [];
  } catch {
    return [];
  }
}

export function savePendingWrites(writes) {
  localStorage.setItem(KEYS.PENDING_WRITES, JSON.stringify(writes));
}
```

**3. `js/modules/state.js`** — Estado global in-memory. Implementar um store simples com getter/setter:

```javascript
let state = {
  config: null,
  categories: null,
  paymentMethods: null,
  currentView: null,
  currentMonth: null,
  isLoading: false
};

export function getState() {
  return state;
}

export function setState(partial) {
  state = { ...state, ...partial };
}

export function resetState() {
  state = {
    config: null,
    categories: null,
    paymentMethods: null,
    currentView: null,
    currentMonth: null,
    isLoading: false
  };
}
```

**4. `js/app.js`** — Entry point e navegação SPA. Seguir o padrão "Pattern 3: SPA Navigation" do RESEARCH.md, adaptado para a estrutura DOM do index.html:

- Importar as funções `init*` de cada view module (dashboard, transaction, statement, receipt, settings)
- Importar `checkFirstRun` de `./views/wizard.js` (será criado no Plan 04 — por ora, criar um import condicional ou stub)
- VIEWS object mapeando view name → { init, icon, label } para as 5 abas (per D-01)
- Função `navigate(viewName)`: esconde todas `.view-section`, remove `.active` de todos `.nav-item`, mostra a section `#view-${viewName}`, adiciona `.active` ao nav-item `[data-view="${viewName}"]`, chama `VIEWS[viewName].init()`
- Exportar `navigate` para que outros módulos possam navegar programaticamente
- Função `showAlert(message, type)` que cria elemento `.alert.alert-${type}` no `#alert-container` com auto-dismiss em 5 segundos
- Exportar `showAlert`
- `DOMContentLoaded` listener: attach click handlers a todos `.nav-item` (lê `dataset.view`, chama `navigate()`), depois verifica primeiro acesso
- Para o primeiro acesso: tentar importar wizard.js. Se `checkFirstRun()` retorna true, exibir o wizard-overlay. Caso contrário, `navigate('dashboard')`.
- **IMPORTANTE:** Como wizard.js ainda não existe (Plan 04), usar um fallback: tentar `import()` dinâmico, e se falhar, ir direto para dashboard. Ou: importar `isWizardDone` de storage.js e, se não done, navigate para 'settings'; se done, navigate para 'dashboard'. A lógica completa do wizard será adicionada no Plan 04.

Abordagem recomendada para o stub do wizard:
```javascript
import { isWizardDone } from './modules/storage.js';

// No DOMContentLoaded:
if (!isWizardDone()) {
  navigate('settings');
} else {
  navigate('dashboard');
}
```

Isso é funcional agora e será aprimorado no Plan 04 quando wizard.js for criado.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); ['js/app.js','js/modules/format.js','js/modules/storage.js','js/modules/state.js'].forEach(f => { const ok = fs.existsSync(f); console.log(f + ': ' + (ok ? 'EXISTS' : 'MISSING')); });"</automated>
  </verify>

  <acceptance_criteria>
    - js/modules/format.js exporta `formatCurrency`, `formatDate`, `formatDateTime`, `getCurrentYearMonth`
    - js/modules/format.js contém `Intl.NumberFormat('pt-BR'` e `currency: 'BRL'`
    - js/modules/format.js contém `Intl.DateTimeFormat('pt-BR'` e `timeZone: 'America/Sao_Paulo'`
    - js/modules/storage.js exporta `getRepoConfig`, `saveRepoConfig`, `isWizardDone`, `markWizardDone`
    - js/modules/storage.js contém `financeirovk_repo` e `financeirovk_wizard_done` como chaves de localStorage
    - js/modules/state.js exporta `getState`, `setState`, `resetState`
    - js/app.js contém `export function navigate(viewName)` ou `export function navigate(`
    - js/app.js contém `export function showAlert(` ou equivalente
    - js/app.js contém `DOMContentLoaded` listener
    - js/app.js importa de `./modules/storage.js` e referencia `isWizardDone`
    - js/app.js contém `data-view` ou `dataset.view` na lógica de click handling
    - Todos os arquivos usam ES Module syntax (`export function`, `import`)
  </acceptance_criteria>

  <done>4 módulos JS core criados com exports corretos, formatação pt-BR via Intl API, localStorage helpers, state management, e navegação SPA funcional</done>
</task>

<task type="auto">
  <name>Task 3: Criar data-service.js e views placeholder</name>
  <files>js/modules/data-service.js, js/views/dashboard.js, js/views/transaction.js, js/views/statement.js, js/views/receipt.js, js/views/settings.js</files>

  <read_first>
    - js/app.js (recém-criado — para ver como navigate() chama init functions, exports esperados)
    - .planning/phases/01-funda-o-pipeline-de-dados/01-RESEARCH.md (seção "Pattern 1: Data Service Layer" — getConfig, getCategories, getTransactions, getPaymentMethods, invalidateCache)
    - .planning/phases/01-funda-o-pipeline-de-dados/01-UI-SPEC.md (seção "Copywriting Contract: Estados Vazios" — heading e body text exatos para cada view placeholder)
  </read_first>

  <action>
Criar 6 arquivos:

**1. `js/modules/data-service.js`** — Camada de acesso a dados. Copiar o padrão EXATO da seção "Pattern 1: Data Service Layer" do RESEARCH.md:

```javascript
const BASE_URL = '.';
const cache = new Map();

export async function getConfig() {
  return fetchJSON(`${BASE_URL}/data/config.json`, 'config');
}

export async function getCategories() {
  return fetchJSON(`${BASE_URL}/data/categories.json`, 'categories');
}

export async function getTransactions(yearMonth) {
  return fetchJSON(
    `${BASE_URL}/data/transactions/${yearMonth}.json`,
    `txn-${yearMonth}`
  );
}

export async function getPaymentMethods() {
  return fetchJSON(`${BASE_URL}/data/payment-methods.json`, 'payment-methods');
}

async function fetchJSON(url, cacheKey) {
  if (cache.has(cacheKey)) return cache.get(cacheKey);
  try {
    const resp = await fetch(url);
    if (!resp.ok) return null;
    const data = await resp.json();
    cache.set(cacheKey, data);
    return data;
  } catch {
    return null;
  }
}

export function invalidateCache(key) {
  if (key) cache.delete(key);
  else cache.clear();
}
```

**2-6. View placeholders** — Cada view exporta uma função `init<Name>()` que renderiza o placeholder no HTML. Usar EXATAMENTE o copy do UI-SPEC seção "Copywriting Contract: Estados Vazios":

**`js/views/dashboard.js`:**
```javascript
export function initDashboard() {
  const section = document.getElementById('view-dashboard');
  if (section.dataset.loaded) return;
  section.innerHTML = `
    <div class="placeholder-view">
      <div class="placeholder-icon">📊</div>
      <h2>Seu painel financeiro</h2>
      <p>Os resumos e gráficos aparecerão aqui quando você registrar suas primeiras transações.</p>
    </div>
  `;
  section.dataset.loaded = 'true';
}
```

**`js/views/transaction.js`:**
- Ícone: 💳
- Heading: "Nova transação"
- Body: "O formulário de registro de transações será habilitado na próxima fase."
- Export: `initTransaction`

**`js/views/statement.js`:**
- Ícone: 📋
- Heading: "Extrato mensal"
- Body: "A lista de transações com filtros será habilitada na próxima fase."
- Export: `initStatement`

**`js/views/receipt.js`:**
- Ícone: 📸
- Heading: "Leitura de comprovantes"
- Body: "O scanner de comprovantes e faturas com IA será habilitado em uma fase futura."
- Export: `initReceipt`

**`js/views/settings.js`:**
- Ícone: ⚙️
- Heading: "Configurações"
- Body: "Configure suas preferências usando o assistente de boas-vindas."
- Export: `initSettings`
- **Nota:** settings.js será expandido no Plan 04 para integrar o wizard. Por ora, renderizar o placeholder.

Cada view segue o mesmo padrão: verifica `section.dataset.loaded` para não re-renderizar, insere HTML com classes `.placeholder-view`, `.placeholder-icon`, heading e parágrafo.
  </action>

  <verify>
    <automated>node -e "const fs=require('fs'); ['js/modules/data-service.js','js/views/dashboard.js','js/views/transaction.js','js/views/statement.js','js/views/receipt.js','js/views/settings.js'].forEach(f => { const ok = fs.existsSync(f); console.log(f + ': ' + (ok ? 'EXISTS' : 'MISSING')); });"</automated>
  </verify>

  <acceptance_criteria>
    - js/modules/data-service.js exporta `getConfig`, `getCategories`, `getTransactions`, `getPaymentMethods`, `invalidateCache`
    - js/modules/data-service.js contém `const cache = new Map()` e `async function fetchJSON(`
    - js/modules/data-service.js contém `data/config.json` e `data/categories.json` e `data/transactions/` nos URLs
    - js/views/dashboard.js exporta `initDashboard` e contém "Seu painel financeiro"
    - js/views/transaction.js exporta `initTransaction` e contém "Nova transação"
    - js/views/statement.js exporta `initStatement` e contém "Extrato mensal"
    - js/views/receipt.js exporta `initReceipt` e contém "Leitura de comprovantes"
    - js/views/settings.js exporta `initSettings` e contém "Configurações"
    - Todos os view files usam `document.getElementById('view-*')` para acessar sua section
    - Todos os view files usam classes CSS `.placeholder-view` e `.placeholder-icon`
  </acceptance_criteria>

  <done>data-service.js criado com fetch + cache para todos endpoints JSON, e 5 views placeholder criadas com copy exato do UI-SPEC</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GitHub Pages CDN | fetch() lê JSON files servidos pelo Pages — dados públicos, sem autenticação |
| Browser localStorage | PAT e config do repo armazenados em localStorage — acessível via DevTools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | js/modules/storage.js (localStorage) | accept | PAT armazenado em localStorage é acessível via DevTools. Aceito para uso pessoal — repo privado + URL obscura mitigam exposição. Fine-Grained PAT com escopo mínimo limita impacto |
| T-01-04 | Tampering | js/modules/data-service.js (fetch) | accept | Dados lidos via fetch podem ser manipulados por MITM, mas HTTPS do Pages protege integridade em trânsito. Dados são display-only nesta fase |
| T-01-05 | Spoofing | index.html (no auth) | accept | App sem autenticação por design (uso pessoal). URL obscura de repo privado como única barreira. Documentado em PROJECT.md como out-of-scope |
</threat_model>

<verification>
1. `index.html` abre no browser sem erros JavaScript no console
2. Sidebar mostra 5 abas com ícones corretos (📊 💳 📋 📸 ⚙️)
3. Clicar em cada aba troca a view visível e marca o nav-item como active
4. Em viewport mobile (≤768px), sidebar vira bottom bar com ícones
5. Cada view mostra placeholder com heading e body text corretos
6. `js/modules/format.js` formata corretamente: `formatCurrency(1234.56)` → `R$ 1.234,56`
7. Todos os arquivos JS usam ES Module syntax (import/export)
</verification>

<success_criteria>
- App carrega no browser com sidebar funcional de 5 abas
- Navegação SPA funciona sem reload de página
- Layout responsivo: sidebar no desktop, bottom bar no mobile
- Formatação brasileira operacional (moeda, data)
- Data service lê JSON sem erros (retorna null se arquivo não existe)
- Todos os 11 arquivos existem com exports corretos
</success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-pipeline-de-dados/01-02-SUMMARY.md`
</output>
